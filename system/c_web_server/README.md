Compile :

```
gcc -Wall -static main.c -o main
```

Launch with param:

```
./main Mathieu
```

Query:

```
curl http://127.0.0.1:8080
```

Docker :

```
sudo docker build -t superboum/amd64_cweb .
```

